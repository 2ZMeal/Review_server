spring:
  application:
    name: review-service

  config:
    import: "configserver:"

  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: false
      retry:
        max-attempts: 3
        initial-interval: 1000
